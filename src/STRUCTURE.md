```angular2html
src/
├── api/                                   # 🔗 API 接口层，负责网络请求和数据交互
│   ├── anlaxy/                            # 🚀 第一个独立后端系统的接口模块
│   │   ├── http.ts                        # axios 实例配置（baseURL、拦截器等）
│   │   ├── endpoint/                      # 实际接口函数定义目录
│   │   │   ├── attendance.ts              # 例：考勤相关接口
│   │   │   ├── leave.ts                   # 例：请假相关接口
│   │   │   └── index.ts                   # 导出该模块所有接口
│   │   ├── types/                         # 请求/响应类型定义（仅限该模块）
│   │   │   ├── attendance.d.ts
│   │   │   ├── leave.d.ts
│   │   │   └── index.d.ts
│   │   └── index.ts                       # 汇总 anlaxy 模块导出
│   │
│   ├── oc/                                # 🧩 第二个后端系统接口模块
│   │   ├── http.ts
│   │   ├── endpoint/
│   │   ├── types/
│   │   └── index.ts
│   │
│   ├── moyi/                              # 🌐 第三个后端系统接口模块
│   │   ├── http.ts
│   │   ├── endpoint/
│   │   ├── types/
│   │   └── index.ts
│   │
│   ├── utils/                             # 工具类封装，如签名、加密、header 附加等
│   │   └── index.ts
│   │
│   └── index.ts                           # 统一导出所有 API 模块，便于全局调用
│
├── assets/                                # 🖼️ 静态资源文件夹（图片、图标、字体等）
│   ├── images/
│   ├── icons/
│   └── fonts/
│
├── components/                            # 🧩 公共组件（基础组件 + 页面专属组件）
│   ├── common/                            # 全局通用组件（Base 开头，通用样式逻辑）
│   │   ├── BaseButton.vue
│   │   ├── BaseCard.vue
│   │   ├── BaseModal.vue
│   │   └── index.ts
│   │
│   └── pages/                             # 页面级组件（仅属于某个页面）
│       ├── home/
│       │   ├── HomeBanner.vue
│       │   ├── HomeStats.vue
│       │   └── index.ts
│       ├── attendance/
│       │   ├── ClassCard.vue
│       │   ├── ClassContainer.vue
│       │   └── index.ts
│       └── login/
│           ├── LoginForm.vue
│           └── index.ts
│
├── composables/                           # 🧠 组合式函数（Composition API）
│   ├── common/                            # 通用逻辑封装（跨页面复用）
│   │   ├── useApiCall.ts
│   │   ├── useLoading.ts
│   │   ├── useUserInfo.ts
│   │   └── index.ts
│   │
│   └── pages/                             # 页面专属逻辑封装（独立于 store）
│       ├── home/
│       │   └── useHomeData.ts
│       ├── attendance/
│       │   ├── useAttendanceList.ts
│       │   ├── useLeaveForm.ts
│       │   └── index.ts
│       └── login/
│           └── useLoginForm.ts
│
├── router/                                # 🧭 路由配置模块
│   ├── modules/                           # 按页面/模块划分的路由配置
│   │   ├── home.ts
│   │   ├── login.ts
│   │   ├── attendance.ts
│   │   └── index.ts
│   └── index.ts                           # 创建 Router 实例并统一导出
│
├── store/                                 # 📦 Pinia 状态管理
│   ├── modules/                           # 各功能模块独立的 store
│   │   ├── user.ts                        # 用户信息状态
│   │   ├── settings.ts                    # 系统设置状态
│   │   ├── attendance.ts                  # 考勤模块状态
│   │   └── index.ts
│   └── index.ts                           # 创建 Pinia 实例、全局注册
│
├── styles/                                # 🎨 全局样式与 SCSS 变量
│   ├── variables.scss                     # 全局 SCSS 变量定义（颜色、间距等）
│   ├── mixins.scss                        # SCSS 混入（mixin）
│   ├── common.scss                        # 全局通用样式（字体、布局等）
│   └── index.scss                         # 样式主入口（全局引入）
│
├── utils/                                 # 🛠️ 工具函数（与业务逻辑无关）
│   ├── constants.ts                       # 全局常量
│   ├── dateTime.ts                        # 日期/时间处理工具
│   ├── errorHandler.ts                    # 全局错误处理逻辑
│   ├── storage.ts                         # 本地存储封装（localStorage/sessionStorage）
│   └── index.ts                           # 工具统一导出
│
├── views/                                 # 🧱 页面视图（对应路由页面）
│   ├── home/
│   │   ├── PageHome.vue                   # 首页页面
│   │   ├── components/                    # 首页专属组件
│   │   ├── style.scss                     # 页面样式
│   │   └── types.d.ts                     # 页面专属类型定义
│   │
│   ├── login/
│   │   ├── PageLogin.vue                  # 登录页面
│   │   ├── style.scss
│   │   └── types.d.ts
│   │
│   ├── attendance/
│   │   ├── AttendanceList.vue             # 考勤列表页面
│   │   ├── LeavePage.vue                  # 请假管理页面
│   │   ├── components/
│   │   ├── style.scss
│   │   └── types.d.ts
│   │
│   └── dev/                               # 🧩 开发测试页面（非正式环境使用）
│       ├── DevHome.vue
│       ├── components/
│       └── style.scss
│
├── App.vue                                # 应用主入口组件
├── main.ts                                # 应用入口文件（创建 App 实例）
├── vite-env.d.ts                          # Vite 环境类型声明
└── shims.d.ts                             # 额外类型声明（如 *.vue 文件类型定义）

```